"use client";

import { useMemo, useState } from "react";
import { ActionIcon, Avatar, Badge, Box, Button, Card, Divider, Drawer, FileInput, Group, Menu, Modal, NumberInput, ScrollArea, Select, SimpleGrid, Stack, Table, Text, TextInput } from "@mantine/core";
import { notifications } from "@mantine/notifications";
import { IconCheck, IconDotsVertical, IconEdit, IconMail, IconPhone, IconPlus, IconTrash, IconUpload, IconUserCheck, IconUsersGroup } from "@tabler/icons-react";
import { DetailItem, MetricCard, PageHeader, StatusBadge } from "@/components/ui/admin";
import { formatCurrency, formatDate } from "@/lib/format";
import { useAdminStore } from "@/store/admin";
import type { Staff } from "@/types/admin";
import { initials } from "../finance/utils";

const blankStaff = (): Staff => ({ id: 0, fullName: "", email: "", phone: "", role: "Guru", classes: [], subjects: [], bankAccount: "", bankAccountName: "", bank: "BCA", birthDate: "", photoUrl: "", status: "Active", maritalStatus: "Belum menikah", city: "", hskLevel: "—", baseSalary: 0, allowance: 0 });

export default function StaffPage() {
  const staff = useAdminStore((state) => state.staff);
  const save = useAdminStore((state) => state.saveStaff);
  const remove = useAdminStore((state) => state.deleteStaff);
  const [editing, setEditing] = useState<Staff | null | undefined>(undefined);
  const [selected, setSelected] = useState<Staff | null>(null);
  const [form, setForm] = useState<Staff>(blankStaff());
  const [query, setQuery] = useState("");
  const [role, setRole] = useState("Semua peran");
  const [status, setStatus] = useState("Semua status");
  const [sortBy, setSortBy] = useState("Nama A–Z");
  const [photoFile, setPhotoFile] = useState<File | null>(null);
  const filtered = useMemo(() => staff.filter((item) => (role === "Semua peran" || item.role === role) && (status === "Semua status" || item.status === status) && `${item.fullName} ${item.email} ${item.city}`.toLowerCase().includes(query.toLowerCase())).sort((a, b) => sortBy === "Nama Z–A" ? b.fullName.localeCompare(a.fullName) : sortBy === "Gaji terbesar" ? (b.baseSalary + b.allowance) - (a.baseSalary + a.allowance) : a.fullName.localeCompare(b.fullName)), [query, role, sortBy, staff, status]);
  const open = (item?: Staff) => { setForm(item ? { ...item } : blankStaff()); setPhotoFile(null); setEditing(item ?? null); };
  const update = <K extends keyof Staff>(key: K, value: Staff[K]) => setForm((current) => ({ ...current, [key]: value }));
  const handlePhoto = (file: File | null) => { setPhotoFile(file); if (!file) return; const reader = new FileReader(); reader.onload = () => update("photoUrl", String(reader.result)); reader.readAsDataURL(file); };
  const submit = () => { if (!form.fullName.trim() || !form.email.trim() || !form.phone.trim() || !form.bankAccount.trim() || !form.bankAccountName.trim() || !form.birthDate || !form.city.trim()) { notifications.show({ color: "red", message: "Lengkapi seluruh data wajib karyawan/guru." }); return; } save(form); setEditing(undefined); notifications.show({ color: "teal", message: "Data karyawan/guru berhasil disimpan." }); };

  return <><PageHeader eyebrow="Tim" title="Karyawan & guru" description="Kelola profil, kompetensi pengajar, rekening, dan status seluruh tim NAO Group." action={<Button className="primary-action" leftSection={<IconPlus size={16} />} onClick={() => open()}>Tambah data</Button>} /><SimpleGrid cols={{ base: 1, xs: 2, xl: 4 }} mb="lg"><MetricCard label="Total tim" value={String(staff.length)} delta="semua peran" icon={IconUsersGroup} tone="gold" /><MetricCard label="Aktif" value={String(staff.filter((item) => item.status === "Active").length)} delta="siap dijadwalkan" icon={IconUserCheck} tone="green" /><MetricCard label="Guru" value={String(staff.filter((item) => item.role === "Guru").length)} delta={`${staff.filter((item) => item.role === "Guru" && item.status === "Active").length} aktif`} icon={IconUsersGroup} tone="purple" /><MetricCard label="Payroll aktif" value={formatCurrency(staff.filter((item) => item.status === "Active").reduce((sum, item) => sum + item.baseSalary + item.allowance, 0))} delta="gaji + tunjangan" icon={IconCheck} /></SimpleGrid><Card className="surface-card filter-card" p="lg" mb="lg"><Group align="flex-end" wrap="wrap"><TextInput label="Cari tim" placeholder="Nama, email, atau kota" value={query} onChange={(event) => setQuery(event.currentTarget.value)} flex={1} miw={220} /><Select label="Peran" value={role} onChange={(value) => setRole(value ?? "Semua peran")} data={["Semua peran", "Guru", "Karyawan", "C-Level"]} w={170} /><Select label="Status" value={status} onChange={(value) => setStatus(value ?? "Semua status")} data={["Semua status", "Active", "Inactive"]} w={155} /><Select label="Urutkan" value={sortBy} onChange={(value) => setSortBy(value ?? "Nama A–Z")} data={["Nama A–Z", "Nama Z–A", "Gaji terbesar"]} w={170} /></Group></Card><Card className="surface-card table-card" p={0}><Group p="lg"><Box><Text className="section-title">Direktori tim</Text><Text size="xs" c="dimmed">{filtered.length} orang · klik baris untuk melihat detail</Text></Box></Group><ScrollArea><Table miw={1150} verticalSpacing="md" horizontalSpacing="lg" highlightOnHover><Table.Thead><Table.Tr><Table.Th>Nama</Table.Th><Table.Th>Peran</Table.Th><Table.Th>Kontak</Table.Th><Table.Th>Kelas & subjek</Table.Th><Table.Th>Level bahasa</Table.Th><Table.Th>Rekening</Table.Th><Table.Th>Status</Table.Th><Table.Th /></Table.Tr></Table.Thead><Table.Tbody>{filtered.map((item) => <Table.Tr key={item.id} className="clickable-row" tabIndex={0} onClick={() => setSelected(item)} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") setSelected(item); }}><Table.Td><Group gap="sm"><Avatar src={item.photoUrl || undefined} color="yellow" radius="xl">{initials(item.fullName)}</Avatar><Box><Text size="sm" fw={600}>{item.fullName}</Text><Text size="xs" c="dimmed">{item.city} · {formatDate(item.birthDate)}</Text></Box></Group></Table.Td><Table.Td><Badge variant="outline" color={item.role === "Guru" ? "yellow" : item.role === "C-Level" ? "dark" : "blue"}>{item.role}</Badge></Table.Td><Table.Td><Group gap={5}><IconMail size={13} /><Text size="xs">{item.email}</Text></Group><Group gap={5} mt={4}><IconPhone size={13} /><Text size="xs">{item.phone}</Text></Group></Table.Td><Table.Td><Text size="sm" fw={600}>{item.classes.length ? item.classes.join(", ") : "—"}</Text><Text size="xs" c="dimmed">{item.subjects.length ? item.subjects.join(", ") : "Tidak mengajar"}</Text></Table.Td><Table.Td>{item.hskLevel}</Table.Td><Table.Td><Text size="sm" fw={600}>{item.bank}</Text><Text size="xs" c="dimmed">•••• {item.bankAccount.slice(-4)} · {item.bankAccountName}</Text></Table.Td><Table.Td><StatusBadge status={item.status} /></Table.Td><Table.Td onClick={(event) => event.stopPropagation()}><Menu position="bottom-end"><Menu.Target><ActionIcon variant="subtle" color="gray"><IconDotsVertical size={17} /></ActionIcon></Menu.Target><Menu.Dropdown><Menu.Item leftSection={<IconEdit size={15} />} onClick={() => open(item)}>Edit</Menu.Item><Menu.Item color="red" leftSection={<IconTrash size={15} />} onClick={() => { if (window.confirm("Hapus data ini?")) remove(item.id); }}>Hapus</Menu.Item></Menu.Dropdown></Menu></Table.Td></Table.Tr>)}</Table.Tbody></Table></ScrollArea></Card><Drawer opened={Boolean(selected)} onClose={() => setSelected(null)} position="right" size="md" title={<Text className="section-title">Detail anggota tim</Text>}>{selected && <Stack gap="lg"><Group className="detail-hero" p="lg"><Avatar src={selected.photoUrl || undefined} size={58} color="yellow" radius="xl">{initials(selected.fullName)}</Avatar><Box><Text className="entity-title">{selected.fullName}</Text><Text size="sm" c="dimmed">{selected.role} · {selected.status}</Text></Box></Group><SimpleGrid cols={2}><DetailItem label="Email" value={selected.email} /><DetailItem label="Nomor HP" value={selected.phone} /><DetailItem label="Asal kota" value={selected.city} /><DetailItem label="Tanggal lahir" value={formatDate(selected.birthDate)} /><DetailItem label="Status pernikahan" value={selected.maritalStatus} /><DetailItem label="Kemampuan bahasa" value={selected.hskLevel} /></SimpleGrid>{selected.role === "Guru" && <><Divider /><Box><Text className="section-title" mb="sm">Pengajaran</Text><Text size="sm">Kelas: {selected.classes.join(", ") || "—"}</Text><Text size="sm" c="dimmed">Subjek: {selected.subjects.join(", ") || "—"}</Text></Box></>}<Divider /><Box><Text className="section-title" mb="sm">Rekening & payroll</Text><Text fw={600}>{selected.bank} · {selected.bankAccount}</Text><Text size="sm" c="dimmed">a.n. {selected.bankAccountName}</Text><Text mt="md" fw={700}>{formatCurrency(selected.baseSalary + selected.allowance)} / bulan</Text></Box><Button variant="light" color="dark" leftSection={<IconEdit size={16} />} onClick={() => { open(selected); setSelected(null); }}>Edit data</Button></Stack>}</Drawer><Modal opened={editing !== undefined} onClose={() => setEditing(undefined)} size="xl" centered title={<Text className="section-title">{editing ? "Edit data tim" : "Tambah karyawan/guru"}</Text>}><Stack><Group align="center"><Avatar src={form.photoUrl || undefined} size={74} color="yellow" radius="xl">{form.fullName ? initials(form.fullName) : "NA"}</Avatar><FileInput label="Foto" placeholder="Upload JPG/PNG" accept="image/png,image/jpeg" value={photoFile} onChange={handlePhoto} leftSection={<IconUpload size={16} />} flex={1} /></Group><SimpleGrid cols={{ base: 1, sm: 2 }}><TextInput label="Nama lengkap" required value={form.fullName} onChange={(event) => update("fullName", event.currentTarget.value)} /><TextInput label="Email" type="email" required value={form.email} onChange={(event) => update("email", event.currentTarget.value)} /><TextInput label="Nomor HP" required value={form.phone} onChange={(event) => update("phone", event.currentTarget.value)} /><Select label="Peran" required value={form.role} onChange={(value) => update("role", (value ?? "Guru") as Staff["role"])} data={["Guru", "Karyawan", "C-Level"]} /><TextInput label="Tanggal lahir" type="date" required value={form.birthDate} onChange={(event) => update("birthDate", event.currentTarget.value)} /><TextInput label="Asal kota" required value={form.city} onChange={(event) => update("city", event.currentTarget.value)} /><Select label="Status pernikahan" value={form.maritalStatus} onChange={(value) => update("maritalStatus", (value ?? "Belum menikah") as Staff["maritalStatus"])} data={["Belum menikah", "Menikah"]} /><Select label="Status" value={form.status} onChange={(value) => update("status", (value ?? "Active") as Staff["status"])} data={["Active", "Inactive"]} /></SimpleGrid>{form.role === "Guru" && <Card withBorder radius="md" p="md"><Text fw={600} mb="sm">Data pengajaran</Text><SimpleGrid cols={{ base: 1, sm: 2 }}><TextInput label="Kelas yang ditangani" description="Pisahkan dengan koma" value={form.classes.join(", ")} onChange={(event) => update("classes", event.currentTarget.value.split(",").map((value) => value.trim()).filter(Boolean))} /><TextInput label="Subjek" description="Pisahkan dengan koma" value={form.subjects.join(", ")} onChange={(event) => update("subjects", event.currentTarget.value.split(",").map((value) => value.trim()).filter(Boolean))} /><Select label="Kemampuan bahasa guru" value={form.hskLevel} onChange={(value) => update("hskLevel", value ?? "—")} data={["—", "HSK 1", "HSK 2", "HSK 3", "HSK 4", "HSK 5", "HSK 6"]} /></SimpleGrid></Card>}<Card withBorder radius="md" p="md"><Text fw={600} mb="sm">Rekening & payroll</Text><SimpleGrid cols={{ base: 1, sm: 2 }}><Select label="Bank" required searchable value={form.bank} onChange={(value) => update("bank", value ?? "BCA")} data={["BCA", "BNI", "BRI", "Mandiri", "CIMB Niaga", "BSI", "Permata"]} /><TextInput label="Nomor rekening" required value={form.bankAccount} onChange={(event) => update("bankAccount", event.currentTarget.value)} /><TextInput label="Rekening atas nama" required value={form.bankAccountName} onChange={(event) => update("bankAccountName", event.currentTarget.value.toUpperCase())} /><NumberInput label="Gaji pokok" min={0} value={form.baseSalary} onChange={(value) => update("baseSalary", Number(value) || 0)} prefix="Rp " thousandSeparator="." decimalSeparator="," /><NumberInput label="Tunjangan tetap" min={0} value={form.allowance} onChange={(value) => update("allowance", Number(value) || 0)} prefix="Rp " thousandSeparator="." decimalSeparator="," /></SimpleGrid>{form.bank !== "BCA" && <Text size="xs" c="yellow.8" mt="sm">Penerima menanggung admin fee Rp2.500 pada setiap payroll.</Text>}</Card><Group justify="flex-end"><Button variant="subtle" color="gray" onClick={() => setEditing(undefined)}>Batal</Button><Button className="primary-action" leftSection={<IconCheck size={16} />} onClick={submit}>Simpan data</Button></Group></Stack></Modal></>;
}
